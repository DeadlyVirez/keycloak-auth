# Info about bcrypt.cjs file

This file is needed to create a hashed string from a password that is statically defined in the script.

The password hash can then be inserted into the `/docker/dex/dex-config.yaml` file