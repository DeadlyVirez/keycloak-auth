# Info about the dex-config.yaml

The `dex-config.yaml` is used for automatic Dex configuration at startup of the container.

It defines a client (Keycloak) and a sample user that can be used to login